% set section titles to sans serif
\usepackage{sectsty}
\usepackage{pdflscape}	% allows change of orientation for single pages (also automatically rotates PDF output)

\allsectionsfont{\sffamily}

\usepackage{fancyhdr}
\usepackage[utf8]{inputenc}	% Umlaute unter UTF8 nutzen
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{anyfontsize}
\usepackage{courier}
\usepackage[shortlabels]{enumitem}	% \begin{enumerate}[a)] % a), b), c), ...
\usepackage{pdfpages} % PDF Dateien einfügen
\usepackage[title]{appendix}
\usepackage{caption}
\usepackage{lipsum}
\newcommand\skipAfterFigure{-16}	% skip in pt for smaller space after image
\newcommand\lb{\\[11 pt]}	% linebreak with 1 additional empty line
\fancypagestyle{completelyEmpty}{\fancyhf{}\renewcommand{\headrulewidth}{0pt}}
\usepackage{titlesec} % allows for the modification of paragraphs (add numbering etc.)
\usepackage[bottom]{footmisc} % places the footer at the bottom of the page
\setcounter{secnumdepth}{4}	% set numbering for paragraphs
\titleformat{\paragraph}
{\sffamily\normalsize\bfseries}{\theparagraph}{1em}{}	% set sans serif font for paragraphs, setup rest of configuration
\titlespacing*{\paragraph}
{0pt}{3.25ex plus 1ex minus .2ex}{1.5ex plus .2ex}

% Variablen importieren
\input{latex_einstellungen/variablen}

% weitere Pakete für Graphiken und PDFs
\usepackage{graphicx}	% Grafiken aus PNG Dateien einbinden
\usepackage{subcaption}
\usepackage{float}		% Graphiken lokalisieren
\usepackage[export]{adjustbox}	% allows to add alignment keys to 
\usepackage{pdfpages}
% Abkürzungsverzeichnis
\usepackage{acronym}

% Deutsche Sonderzeichen und Silbentrennung nutzen
\usepackage[ngerman]{babel}

% Zeichenencoding1
\usepackage[T1]{fontenc}
\usepackage{lmodern}

% floatende Bilder ermöglichen
%\usepackage{floatflt}

% mehrseitige Tabellen ermöglichen
\usepackage{longtable}
\usepackage{tabu}
\usepackage{tabularx} 
\usepackage{ltablex}
\usepackage{multirow}

% Packet für Seitenrandabständex und Einstellung für Seitenränder
\usepackage{geometry}
\geometry{left=25mm, right=25mm, top=25mm, bottom=25mm}
\setlength{\abovecaptionskip}{1mm}
% Paket für Boxen im Text
\usepackage{fancybox}

% bricht lange URLs "schön" um
\usepackage[hyphens]{url} % possible options: [hyphens,obeyspaces,spaces]

% Paket für Textfarben
\usepackage{color}

% Mathematische Symbole importieren
\usepackage{amssymb}
\usepackage{siunitx}	% SI symbols
\usepackage{mathtools}

% Für Monospace Text (Code, Pseudocode)
\usepackage{fancyvrb}

\makeatletter
\renewcommand\float@endH{\@endfloatbox\vskip\intextsep
  \if@flstyle\setbox\@currbox\float@makebox\columnwidth\fi
  \box\@currbox\vskip\intextsep\relax\@doendpe}
\newcommand*{\textoverline}[1]{$\overline{\hbox{#1}}\m@th$}
\newcolumntype{Y}{>{\centering\arraybackslash}X} % allows centered flexible width columns
\makeatother

% erzeugt Inhaltsverzeichnis mit Querverweisen zu den Abschnitten (PDF Version)
\usepackage[bookmarksnumbered,pdftitle={\titleDocument},hyperfootnotes=false]{hyperref}

% für Tabellen
\usepackage{array}

% Runde Klammern für Zitate
\usepackage[nosectionbib,numberedbib]{apacite}
\AtBeginDocument{\renewcommand\refname{Quellen- und Literaturverzeichnis}}

% Paket für Zeilenabstand
\usepackage{setspace}

% für Bildbezeichner
\usepackage{capt-of}

% für Stichwortverzeichnistype
\usepackage{makeidx}

% für Listings/Code
\usepackage{listings}
\lstset{numbers=left, numberstyle=\tiny, numbersep=5pt, keywordstyle=\color{black}\bfseries, stringstyle=\ttfamily,showstringspaces=false,basicstyle=\footnotesize,captionpos=b}
\lstset{language=java}

% Indexerstellung
\makeindex

% Kopf- und Fusszeile festlegen
\newpage
\pagestyle{fancy}
\setcounter{page}{1}	% Seitenzahl auf 1 forcieren
\rhead{}
\lhead{\titleDocument}
\renewcommand{\headrulewidth}{0.4pt}% default is 0pt
\renewcommand{\footrulewidth}{0.4pt}% default is 0pt
\cfoot{Seite \thepage}